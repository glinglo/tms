// Free tier definition — single source of truth for copy across the site
export const FREE_LEADS_PER_MONTH = 50

// CTA destination: all conversion CTAs open the signup modal via useAuthContext().openSignUp()
// This constant is kept for reference; LandingHero and LandingCTA use openSignUp() internally.
export const CTA_HREF = '/'
